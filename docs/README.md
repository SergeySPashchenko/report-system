# Documentation Index

## 📚 Available Documentation

### [User API Documentation](./User.md)
Повна документація API для управління користувачами:
- Структура проекту
- Архітектурні шари
- Приклади використання
- Тестування
- Best practices

### [Company API Documentation](./Company.md)
Повна документація API для управління компаніями:
- Структура проекту
- Система доступу через Access модель
- Автоматичне призначення компанії "Main"
- Приклади використання
- Тестування

### [Brand API Documentation](./Brand.md)
Повна документація API для управління брендами:
- Структура проекту
- Система доступу через Access модель
- Приклади використання
- Тестування

### [Product API Documentation](./Product.md)
Повна документація API для управління продуктами:
- Структура проекту
- Система доступу з підтримкою різних рівнів доступу
- Синхронізація з зовнішньою базою даних
- Приклади використання
- Тестування

### [Category API Documentation](./Category.md)
Повна документація API для управління категоріями:
- Структура проекту
- Публічний доступ для всіх користувачів
- Синхронізація з зовнішньою базою даних
- Приклади використання
- Тестування

### [Gender API Documentation](./Gender.md)
Повна документація API для управління гендерами:
- Структура проекту
- Публічний доступ для всіх користувачів
- Синхронізація з зовнішньою базою даних
- Приклади використання
- Тестування

### [Product Item API Documentation](./ProductItem.md)
Повна документація API для управління product items:
- Структура проекту
- Система доступу з підтримкою різних рівнів доступу
- Синхронізація з зовнішньою базою даних
- Nested routes під products
- Приклади використання
- Тестування

### [Expense API Documentation](./Expense.md)
Повна документація API для управління expenses:
- Структура проекту
- Система доступу з підтримкою різних рівнів доступу
- Синхронізація з зовнішньою базою даних
- Nested routes під products, brands, categories, genders, expensetypes
- Приклади використання
- Тестування

### [Expensetype API Documentation](./Expensetype.md)
Повна документація API для управління expensetypes:
- Структура проекту
- Публічний доступ для всіх користувачів
- Синхронізація з зовнішньою базою даних
- Nested routes під expensetypes
- Приклади використання
- Тестування

---

## 🎯 Реалізований Функціонал

### Система Користувачів (User)
✅ Повний CRUD для користувачів
✅ Реєстрація та автентифікація
✅ Система подій та слухачів
✅ Observer для автоматичних дій
✅ Політики авторизації
✅ Пошук та пагінація
✅ Статистика користувачів
✅ Soft deletes з можливістю відновлення

### Система Компаній (Company)
✅ Повний CRUD для компаній
✅ Автоматичне створення компанії "Main" для першого користувача
✅ Призначення доступу до компанії "Main" всім наступним користувачам
✅ Поліморфна система доступу через модель Access
✅ Політики авторизації (користувачі можуть керувати тільки своїми компаніями)
✅ Пошук та пагінація
✅ Статистика компаній
✅ Soft deletes з можливістю відновлення
✅ Захист компанії "Main" від видалення

### Система Брендів (Brand)
✅ Повний CRUD для брендів
✅ Поліморфна система доступу через модель Access
✅ Політики авторизації з підтримкою різних рівнів доступу
✅ Пошук та пагінація
✅ Статистика брендів (фільтрована за доступом)
✅ Soft deletes з можливістю відновлення
✅ Користувачі компанії мають доступ до всіх брендів
✅ Користувачі з доступами по брендам бачать тільки свої бренди

### Система Продуктів (Product)
✅ Повний CRUD для продуктів
✅ Поліморфна система доступу через модель Access
✅ Політики авторизації з підтримкою різних рівнів доступу
✅ Пошук та пагінація
✅ Статистика продуктів (фільтрована за доступом)
✅ Soft deletes з можливістю відновлення
✅ Зв'язки з брендами, категоріями та гендерами
✅ Синхронізація з зовнішньою базою даних
✅ Користувачі компанії мають доступ до всіх продуктів
✅ Користувачі з доступами по брендам бачать продукти своїх брендів
✅ Користувачі з доступами по продуктам бачать тільки свої продукти

### Система Категорій (Category)
✅ Повний CRUD для категорій
✅ Публічний доступ для всіх авторизованих користувачів
✅ Пошук та пагінація
✅ Статистика категорій
✅ Soft deletes з можливістю відновлення
✅ Синхронізація з зовнішньою базою даних
✅ Використовується як довідкова інформація для продуктів

### Система Гендерів (Gender)
✅ Повний CRUD для гендерів
✅ Публічний доступ для всіх авторизованих користувачів
✅ Пошук та пагінація
✅ Статистика гендерів
✅ Soft deletes з можливістю відновлення
✅ Синхронізація з зовнішньою базою даних
✅ Використовується як довідкова інформація для продуктів

### Система Доступу (Access)
✅ Поліморфна модель для зв'язку користувачів з ресурсами
✅ Підтримка soft deletes
✅ Зв'язки між User, Company, Brand та Product через Access
✅ Підтримка різних рівнів доступу:
  - Компанія → доступ до всього
  - Бренди → доступ до всіх продуктів брендів
  - Продукти → доступ тільки до призначених продуктів

---

## 🔗 Зв'язки Між Моделями

### User ↔ Company
- **Many-to-Many** через модель `Access`
- Кожен користувач може мати доступ до багатьох компаній
- Кожна компанія може мати багатьох користувачів
- Перший користувач автоматично створює компанію "Main"
- Всі наступні користувачі отримують доступ до тієї ж компанії "Main"

### User ↔ Brand
- **Many-to-Many** через модель `Access`
- Кожен користувач може мати доступ до багатьох брендів
- Кожен бренд може мати багатьох користувачів
- Бренди НЕ створюються автоматично (на відміну від Company)
- Доступ до брендів надається вручну через Access
- Користувачі компанії автоматично мають доступ до всіх брендів

### User ↔ Product
- **Many-to-Many** через модель `Access`
- Кожен користувач може мати доступ до багатьох продуктів
- Кожен продукт може мати багатьох користувачів
- Продукти НЕ створюються автоматично
- Доступ до продуктів може надаватися:
  - Через доступ до компанії (всі продукти)
  - Через доступ до брендів (продукти брендів)
  - Через прямий доступ до продуктів

### Product ↔ Brand
- **Many-to-One** через `brand_id`
- Кожен продукт належить одному бренду
- Кожен бренд може мати багато продуктів

### Product ↔ Category
- **Many-to-One** через `main_category_id` та `marketing_category_id`
- Кожен продукт може мати основну та маркетингову категорію
- Кожна категорія може мати багато продуктів

### Product ↔ Gender
- **Many-to-One** через `gender_id`
- Кожен продукт належить одному гендеру
- Кожен гендер може мати багато продуктів

### Access Model
- Поліморфна модель для зв'язку користувачів з різними ресурсами
- Зв'язує `user_id` з `accessible_id` та `accessible_type`
- Підтримує soft deletes
- Підтримує типи: `company`, `brand`, `product`, `user`, `access`

---

## 🚀 API Endpoints

### Authentication
- `POST /api/v1/auth/register` - Реєстрація користувача
- `POST /api/v1/auth/login` - Вхід в систему
- `GET /api/v1/auth/me` - Поточна інформація про користувача
- `POST /api/v1/auth/logout` - Вихід з системи
- `POST /api/v1/auth/logout-all` - Вихід з усіх пристроїв
- `POST /api/v1/auth/refresh` - Оновлення токену

### Users
- `GET /api/v1/users` - Список користувачів
- `POST /api/v1/users` - Створення користувача
- `GET /api/v1/users/{username}` - Отримання користувача
- `PUT/PATCH /api/v1/users/{username}` - Оновлення користувача
- `DELETE /api/v1/users/{username}` - Видалення користувача
- `POST /api/v1/users/{id}/restore` - Відновлення користувача
- `DELETE /api/v1/users/{id}/force` - Остаточне видалення
- `GET /api/v1/users/statistics` - Статистика користувачів

### Companies
- `GET /api/v1/companies` - Список компаній
- `POST /api/v1/companies` - Створення компанії
- `GET /api/v1/companies/{slug}` - Отримання компанії
- `PUT/PATCH /api/v1/companies/{slug}` - Оновлення компанії
- `DELETE /api/v1/companies/{slug}` - Видалення компанії
- `POST /api/v1/companies/{id}/restore` - Відновлення компанії
- `DELETE /api/v1/companies/{id}/force` - Остаточне видалення
- `GET /api/v1/companies/statistics` - Статистика компаній

### Brands
- `GET /api/v1/brands` - Список брендів (фільтрований за доступом)
- `POST /api/v1/brands` - Створення бренду
- `GET /api/v1/brands/{slug}` - Отримання бренду
- `PUT/PATCH /api/v1/brands/{slug}` - Оновлення бренду
- `DELETE /api/v1/brands/{slug}` - Видалення бренду
- `POST /api/v1/brands/{id}/restore` - Відновлення бренду
- `DELETE /api/v1/brands/{id}/force` - Остаточне видалення
- `GET /api/v1/brands/statistics` - Статистика брендів (фільтрована за доступом)

### Products
- `GET /api/v1/products` - Список продуктів (фільтрований за доступом)
- `POST /api/v1/products` - Створення продукту
- `GET /api/v1/products/{slug}` - Отримання продукту
- `PUT/PATCH /api/v1/products/{slug}` - Оновлення продукту
- `DELETE /api/v1/products/{slug}` - Видалення продукту
- `POST /api/v1/products/{id}/restore` - Відновлення продукту
- `DELETE /api/v1/products/{id}/force` - Остаточне видалення
- `GET /api/v1/products/statistics` - Статистика продуктів (фільтрована за доступом)

### Categories
- `GET /api/v1/categories` - Список категорій (доступно всім)
- `POST /api/v1/categories` - Створення категорії
- `GET /api/v1/categories/{slug}` - Отримання категорії
- `PUT/PATCH /api/v1/categories/{slug}` - Оновлення категорії
- `DELETE /api/v1/categories/{slug}` - Видалення категорії
- `POST /api/v1/categories/{id}/restore` - Відновлення категорії
- `DELETE /api/v1/categories/{id}/force` - Остаточне видалення
- `GET /api/v1/categories/statistics` - Статистика категорій

### Genders
- `GET /api/v1/genders` - Список гендерів (доступно всім)
- `POST /api/v1/genders` - Створення гендеру
- `GET /api/v1/genders/{slug}` - Отримання гендеру
- `PUT/PATCH /api/v1/genders/{slug}` - Оновлення гендеру
- `DELETE /api/v1/genders/{slug}` - Видалення гендеру
- `POST /api/v1/genders/{id}/restore` - Відновлення гендеру
- `DELETE /api/v1/genders/{id}/force` - Остаточне видалення
- `GET /api/v1/genders/statistics` - Статистика гендерів

### Product Items
- `GET /api/v1/product-items` - Список product items (фільтрований за доступом)
- `POST /api/v1/product-items` - Створення product item
- `GET /api/v1/product-items/{slug}` - Отримання product item
- `PUT/PATCH /api/v1/product-items/{slug}` - Оновлення product item
- `DELETE /api/v1/product-items/{slug}` - Видалення product item
- `POST /api/v1/product-items/{id}/restore` - Відновлення product item
- `DELETE /api/v1/product-items/{id}/force` - Остаточне видалення
- `GET /api/v1/product-items/statistics` - Статистика product items (фільтрована за доступом)

#### Nested Routes під Products
- `GET /api/v1/products/{product}/product-items` - Список product items для продукту
- `POST /api/v1/products/{product}/product-items` - Створення product item для продукту
- `GET /api/v1/products/{product}/product-items/{product_item}` - Отримання product item для продукту
- `PUT/PATCH /api/v1/products/{product}/product-items/{product_item}` - Оновлення product item для продукту
- `DELETE /api/v1/products/{product}/product-items/{product_item}` - Видалення product item для продукту

#### Nested Routes під Brands
- `GET /api/v1/brands/{brand}/product-items` - Список product items для бренду

#### Nested Routes під Categories
- `GET /api/v1/categories/{category}/product-items` - Список product items для категорії

#### Nested Routes під Genders
- `GET /api/v1/genders/{gender}/product-items` - Список product items для гендеру

### Expenses
- `GET /api/v1/expenses` - Список expenses (фільтрований за доступом)
- `POST /api/v1/expenses` - Створення expense
- `GET /api/v1/expenses/{id}` - Отримання expense
- `PUT/PATCH /api/v1/expenses/{id}` - Оновлення expense
- `DELETE /api/v1/expenses/{id}` - Видалення expense
- `POST /api/v1/expenses/{id}/restore` - Відновлення expense
- `DELETE /api/v1/expenses/{id}/force` - Остаточне видалення
- `GET /api/v1/expenses/statistics` - Статистика expenses (фільтрована за доступом)

#### Nested Routes під Products
- `GET /api/v1/products/{product}/expenses` - Список expenses для продукту
- `POST /api/v1/products/{product}/expenses` - Створення expense для продукту
- `GET /api/v1/products/{product}/expenses/{expense}` - Отримання expense для продукту
- `PUT/PATCH /api/v1/products/{product}/expenses/{expense}` - Оновлення expense для продукту
- `DELETE /api/v1/products/{product}/expenses/{expense}` - Видалення expense для продукту

#### Nested Routes під Brands
- `GET /api/v1/brands/{brand}/expenses` - Список expenses для бренду

#### Nested Routes під Categories
- `GET /api/v1/categories/{category}/expenses` - Список expenses для категорії

#### Nested Routes під Genders
- `GET /api/v1/genders/{gender}/expenses` - Список expenses для гендеру

#### Nested Routes під Expensetypes
- `GET /api/v1/expensetypes/{expensetype}/expenses` - Список expenses для типу витрати
- `POST /api/v1/expensetypes/{expensetype}/expenses` - Створення expense для типу витрати
- `GET /api/v1/expensetypes/{expensetype}/expenses/{expense}` - Отримання expense для типу витрати
- `PUT/PATCH /api/v1/expensetypes/{expensetype}/expenses/{expense}` - Оновлення expense для типу витрати
- `DELETE /api/v1/expensetypes/{expensetype}/expenses/{expense}` - Видалення expense для типу витрати

### Expensetypes
- `GET /api/v1/expensetypes` - Список expensetypes (доступно всім)
- `POST /api/v1/expensetypes` - Створення expensetype
- `GET /api/v1/expensetypes/{slug}` - Отримання expensetype
- `PUT/PATCH /api/v1/expensetypes/{slug}` - Оновлення expensetype
- `DELETE /api/v1/expensetypes/{slug}` - Видалення expensetype
- `POST /api/v1/expensetypes/{id}/restore` - Відновлення expensetype
- `DELETE /api/v1/expensetypes/{id}/force` - Остаточне видалення
- `GET /api/v1/expensetypes/statistics` - Статистика expensetypes

---

## 📋 Автоматичні Процеси

### При Створенні Користувача
1. `UserObserver::creating()` - Логування
2. Користувач створюється в базі даних
3. `UserObserver::created()` - Логування та подія `UserCreated`
4. `AssignCompanyToUser` listener:
   - Перевіряє чи існує компанія "Main"
   - Створює "Main" якщо це перший користувач
   - Створює запис `Access` для зв'язку користувача з компанією
5. **Бренди НЕ створюються автоматично** - доступ до брендів надається вручну

### При Видаленні Користувача
1. `UserObserver::deleting()` - Логування
2. Видаляються всі токени користувача
3. Користувач soft delete
4. `UserObserver::deleted()` - Логування та подія `UserDeleted`

---

## 🛡️ Авторизація

### User Policy
- Всі можуть переглядати список користувачів
- Користувач може редагувати тільки свій профіль
- Користувач не може видалити сам себе

### Company Policy
- Всі можуть переглядати список компаній
- Користувач може керувати тільки компаніями до яких має доступ
- Компанія "Main" не може бути видалена

### Brand Policy
- Всі можуть переглядати список брендів (фільтрований за доступом)
- Користувач компанії може керувати всіма брендами
- Користувач з доступами по брендам може керувати тільки своїми брендами
- Бренди можуть бути видалені (немає захисту як для "Main" компанії)

### Product Policy
- Користувач компанії має доступ до всіх продуктів
- Користувач з доступами по брендам бачить продукти своїх брендів
- Користувач з доступами по продуктам бачить тільки свої продукти
- Всі операції фільтруються за рівнем доступу користувача

### Category Policy
- Всі авторизовані користувачі мають повний доступ до категорій
- Категорії є довідковою інформацією і доступні всім

### Gender Policy
- Всі авторизовані користувачі мають повний доступ до гендерів
- Гендери є довідковою інформацією і доступні всім

### ProductItem Policy
- Користувач компанії має доступ до всіх product items
- Користувач з доступами по продуктам бачить product items своїх продуктів
- Користувач з доступами по брендам бачить product items продуктів своїх брендів
- Всі операції фільтруються за рівнем доступу користувача

### Expense Policy
- Користувач компанії має доступ до всіх expenses
- Користувач з доступами по продуктам бачить expenses своїх продуктів
- Користувач з доступами по брендам бачить expenses продуктів своїх брендів
- Всі операції фільтруються за рівнем доступу користувача

### Expensetype Policy
- Expensetypes є довідковою інформацією і доступні всім

---

## 📦 Структура Файлів

```
app/
├── Events/              # Події системи
├── Listeners/           # Обробники подій
│   └── AssignCompanyToUser.php  # Призначення компанії користувачу
├── Http/
│   ├── Controllers/Api/
│   │   ├── AuthController.php
│   │   ├── UserController.php
│   │   └── CompanyController.php
│   ├── Requests/        # Валідація запитів
│   └── Resources/       # Трансформація даних
├── Models/
│   ├── User.php         # Модель користувача
│   ├── Company.php      # Модель компанії
│   ├── Brand.php        # Модель бренду
│   └── Access.php       # Модель доступу
├── Observers/
│   └── UserObserver.php # Спостерігач за користувачами
├── Policies/            # Політики авторизації
├── Queries/             # Query builders
├── Services/            # Бізнес-логіка
└── Providers/           # Service providers
```

---

## 🧪 Тестування

Всі компоненти мають тести:
- Feature tests для API endpoints
- Unit tests для сервісів та listeners
- Policy tests для авторизації

Запуск тестів:
```bash
php artisan test
```

---

## 📝 Примітки

1. **Компанія "Main"**: Створюється автоматично першим користувачем і не може бути видалена
2. **Access Model**: Використовує поліморфні зв'язки для майбутнього розширення
3. **Slug Generation**: Всі моделі використовують slug для URL (автоматично генерується з назви)
4. **Soft Deletes**: Всі моделі підтримують soft deletes з можливістю відновлення
5. **Rate Limiting**: API має обмеження швидкості запитів
6. **Система Доступу**: 
   - Користувачі компанії мають доступ до всього
   - Користувачі з доступами по брендам бачать тільки свої бренди та їх продукти
   - Користувачі з доступами по продуктам бачать тільки свої продукти
   - Категорії та гендери доступні всім як довідкова інформація
7. **Синхронізація**: Продукти, категорії та гендери можуть бути синхронізовані з зовнішньою базою даних через `php artisan products:sync`

---

## 🔄 Синхронізація Даних

### Команда Синхронізації Продуктів

```bash
php artisan products:sync
```

Ця команда:
1. Синхронізує категорії з зовнішньої бази даних
2. Синхронізує гендери з зовнішньої бази даних
3. Синхронізує продукти з їх зв'язками
4. Знаходить або створює бренди за назвою
5. Зберігає старі ID для мапінгу (`category_id`, `gender_id`, `ProductID`)

### Команда Синхронізації Брендів

```bash
php artisan brands:sync
```

Ця команда синхронізує бренди з зовнішньої бази даних.

---

## 🔄 Майбутні Розширення

Система Access підготовлена для додавання інших типів ресурсів:
- Projects
- Teams
- Departments
- Інші поліморфні ресурси

Просто додайте новий тип до `accessible_type` в моделі Access та до морф-мапи в `AppServiceProvider`.

### Ролі та Дозволи

Після завершення синхронізації планується додати систему ролей для більш гнучкого управління доступами.
